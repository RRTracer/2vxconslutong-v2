# Terms & Notes

These pages are editable markdown files in `src/content`.

## Usage

- Use `src/content/*.md` to author text pages
- The site includes a minimal markdown renderer for headings, lists and paragraphs

Replace content as needed.
